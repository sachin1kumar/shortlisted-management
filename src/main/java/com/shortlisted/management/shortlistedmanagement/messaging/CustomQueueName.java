/*
package com.shortlisted.management.shortlistedmanagement.messaging;

import org.springframework.lang.NonNull;

public class CustomQueueName {

    public static String queueName(@NonNull final String prefix,
                                   @NonNull final String serviceName,
                                   @NonNull final String aggregateRootName) {
        return prefix + '.' + serviceName + '.' + aggregateRootName;
    }
}
*/
