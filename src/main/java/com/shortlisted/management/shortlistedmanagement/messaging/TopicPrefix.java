/*
package com.shortlisted.management.shortlistedmanagement.messaging;

import org.springframework.lang.NonNull;

public enum TopicPrefix {

    COMMAND("command."),
    RESPONSE("response."),
    ERROR("error."),
    STATUS("status.");

    private final String prefix;

    TopicPrefix(@NonNull String prefix) {
        this.prefix = prefix;
    }

    String getPrefix() {
        return prefix;
    }
}
*/
